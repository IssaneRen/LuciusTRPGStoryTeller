---
description: "Vue 组件规范 — 详见 .claude/agents/web-developer.md"
globs: "frontend/src/**/*.vue"
---

快速提醒（权威规范在 .claude/agents/web-developer.md）:
- 禁止 Options API，必须 `<script setup lang="ts">`
- 禁止 any 类型
- 组件 < 200 行，超过则拆分
